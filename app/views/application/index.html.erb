<header class="main-header">
    <div class="mh-inner-div">
        <%= link_to tag("i class='fas fa-arrow-left fa-2x'></i"), user_path(current_user.id), class: "link_i" %>
        <h3>Feed</h3>
    </div>
</header>

<%= display_feed(@feed) %>
<br>