<p>There is your external activities:</p>
<hr>
<% current_user.external_activities.each do |act| %>
<%= act.name %>
<br>
<%= act.description %>
<br>
group name: <%= link_to act.get_group.name, group_path(act.get_group.id) %> <%#= act.get_group.name %>
<p>Posted by <%= act.author.email %> at <%= act.created_at %></p>
<hr>

<% end %>

<%=  link_to "Back to my page", user_path(current_user.id) %>